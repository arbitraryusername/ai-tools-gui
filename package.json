{
  "name": "project-root",
  "private": true,
  "workspaces": ["client", "server"],
  "devDependencies": {
    "typescript": "~5.6.2",
    "eslint": "^9.17.0",
    "prettier": "^2.0.0",
    "concurrently": "^7.0.0"
  },
  "scripts": {
    "build": "concurrently --kill-others \"pnpm --filter server build\" \"pnpm --filter client build\""
  }
}