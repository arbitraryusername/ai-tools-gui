{
  "name": "project-root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0",
    "eslint": "^9.17.0",
    "prettier": "^2.8.8",
    "typescript": "~5.6.3",
    "typescript-eslint": "^8.18.2"
  },
  "scripts": {
    "build": "concurrently --kill-others-on-fail \"pnpm --filter server build\" \"pnpm --filter client build\"",
    "dev": "concurrently --kill-others-on-fail \"pnpm --filter client dev\" \"pnpm --filter server dev\""
  }
}